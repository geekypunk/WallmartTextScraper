package test;

import static org.junit.Assert.assertEquals;
import main.HTMLParser;

import org.junit.Test;

/**
 *  Class containing unit tests for methods in HTMLParser class
 * The file only serves as a place holder for unit tests, no methods have been implemented
 *
 */
public class HTMLParserTest {
	
	private HTMLParser parser = new HTMLParser();
	
	@Test
	public void testHybrid1() {
		
		assertEquals(true, true);
	}

}
